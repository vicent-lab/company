{
  "name": "smart-dairy-farm",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspace @dairy/web --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
