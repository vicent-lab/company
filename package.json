{
  "name": "smart-dairy-farm",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspace @dairy/web --if-present && npm run build --workspace @dairy/server --if-present && mkdir -p apps/server/dist/web && cp -r apps/web/dist/* apps/server/dist/web/ && cp -r apps/server/dist dist",
    "test": "npm run test --workspaces --if-present"
  }
}
